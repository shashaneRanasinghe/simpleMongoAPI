type Query

type Mutation

input Pagination {
    page: Int
    pageSize: Int
}

input SortBy {
    column: String
    direction: Direction
}

enum Direction {
    ASC
    DESC
}

type Test {
    test: String
}